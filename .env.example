# This project runs n8n via Docker; credentials are stored inside n8n, not here.
# A user cloning this repo configures these inside the n8n UI:
#   - Gmail OAuth2 credentials (Google Cloud project)
#   - Groq API key (used in the HTTP Request / OpenAI node)
GROQ_API_KEY=your_groq_api_key_here
